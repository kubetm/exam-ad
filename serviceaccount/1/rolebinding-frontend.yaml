apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: frontend-rolebinding
  namespace: beta
subjects:
  - kind: ServiceAccount
    name: frontend-sa
    namespace: beta
roleRef:
  kind: Role
  name: frontend-role
  apiGroup: rbac.authorization.k8s.io